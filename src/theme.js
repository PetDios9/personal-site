import {createMuiTheme} from '@material-ui/core'

const theme = createMuiTheme({
    typography: {
        fontFamily: [
            'Montserrat'
        ]
    },
    palette: {
        background: {
            default: '#212121'
        }
    }
})

export default theme